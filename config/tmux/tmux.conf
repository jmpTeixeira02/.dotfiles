# Reset keybinds for indeponteny
unbind-key -a -T sesh

set-option -g status-position top
set -g detach-on-destroy off
set -g mouse on
set-window-option mode-keys vi

bind r source-file ~/.config/tmux/tmux.conf \; display "Reloaded tmux config"


# TMUX Sesh
bind-key -n -T sesh s display-popup -B -E "zsh -ic 'sesh-connect-session'"
bind-key -n -T sesh S display-popup -B -E "zsh -ic 'sesh-new-session'"
bind-key -n -T sesh C display-popup -B -E "zsh -ic 'sesh-kill-session'"


unbind %
bind | split-window -h

unbind '"'
bind - split-window -v

set -g default-command "zsh"
